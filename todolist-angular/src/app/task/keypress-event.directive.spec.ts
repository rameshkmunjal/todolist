import { KeypressEventDirective } from './keypress-event.directive';

describe('KeypressEventDirective', () => {
  it('should create an instance', () => {
    const directive = new KeypressEventDirective();
    expect(directive).toBeTruthy();
  });
});
